//const textInCell = (text) => {
      //  return document.createElement("td").innerText = text;
      //}; //textの値を入れたセル要素の作成

      ////dayデータの入ったセルを一つ作成し行に追加;
      //trOftbl.appendChild(textInCell(day));
//
      ////categoryの入ったセルを一つ作成し行に追加
      //trOftbl.appendChild(textInCell(category));
//
      ////contentの入ったセルを一つ作成し行に追加
      //trOftbl.appendChild(textInCell(content));
      //
      //////const tbodyOfTable = document.createElement("tbody");
      //tbl.appendChild(trOftbl);
     //
      
      ///tbodyOfTable.appendChild(tbl);
      //tbl.innnerText = day;
      
      
      
      
      
      ///////////////////////////////////////
      //改行
      
      //tbl.appendChild(tr);
      
      //tr;
      
      //
      ////データの入ったセルを３つ追加
      //const dayOfTable = tbl.appendChild(td);
      //dayOfTable.innerText = day;
      //td.innerText = day;
//
      //const categoryOfTable = tbl.appendChild(td);
      //categoryOfTable.innerText = category;
      //td.innerText = category;
      
      //const contentOfTable = tbl.appendChild(td);
      //contentOfTable.innerText = content;
      
      //td.innerText = content;
      
      //「３個データを入れたセルを作る ⇨ 改行」を５回繰り返す
      
      